# Supabase Auth (publishable values; safe for the browser bundle)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Canonical public origin. Local development can use http://localhost:3000.
NEXT_PUBLIC_BASE_URL=

# Server-only Postgres connection used by My Radius place sync.
DATABASE_URL=
DIRECT_URL=
