# Runner identity and disposable job state use separate Compose-owned named
# volumes. Do not add a host-path setting for either one.

# Fresh hardened registration only. Obtain this one-hour repository
# registration token from GitHub, start the project once, then blank this
# value and rebuild/recreate the project so it is absent from service metadata.
RUNNER_TOKEN=
