# Use the actual protected Container Manager shared-folder path if the NAS
# does not store Docker data on volume1.
RUNNER_STATE_PATH=/volume1/docker/frederick-radius-runner/state

# First start only. Obtain this one-hour repository registration token from
# GitHub, start the project, then blank this value and recreate the container.
RUNNER_TOKEN=
