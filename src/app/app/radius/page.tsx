import ComingSoon from "@/components/today/ComingSoon";
export const metadata = { title: "Radius" };
export default function RadiusPage() {
  return (
    <ComingSoon
      title="Radius"
      description="Set a point, set a distance — see everything inside. A 10-minute walk, a half-mile drive, around a hotel, a trailhead, a parking garage. The signature interaction. Phase 3 (Sprints 7–8)."
    />
  );
}
